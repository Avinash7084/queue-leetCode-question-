#include<iostream>
#include<queue>
#include<stack>
using namespace std;

stack<int> st;

void reverse(queue<int>& q){
    while(st.size() != 0){
        int y = st.top();
        q.push(y);
        cout<<y <<" ";
        st.pop();
    }    
}

int main(){
    queue<int>q;
    
    
    q.push(10);
    q.push(20);
    q.push(30);
    q.push(40);
    q.push(50);
    
    while(q.size()!=0){
        int x = q.front();
        st.push(x);
        q.pop();
    }
    
    reverse(q);
}
